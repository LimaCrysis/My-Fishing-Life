'use strict';
const fs=require('fs'),vm=require('vm'),assert=require('assert'),root=__dirname+'/..';global.window=global;for(const file of['species-presence-shadow-runner.js','species-presence-integration-experiment.js','species-presence-timefactor-experiment.js','species-presence-cm-robustness.js'])vm.runInThisContext(fs.readFileSync(`${root}/js/forecast/${file}`,'utf8'),{filename:file});
assert.equal(MFLSpeciesPresenceCMRobustness.CM_FLOOR,.25);assert.equal(MFLSpeciesPresenceCMRobustness.CM_SLOPE,.75);assert.equal(MFLSpeciesPresenceTimeFactorExperiment.VARIANTS.CM.floor,.25);
const phase7=JSON.parse(fs.readFileSync(`${root}/data/species-presence-shadow-phase7-report.json`)),before=JSON.stringify(phase7),report=MFLSpeciesPresenceCMRobustness.validate(phase7);assert.equal(JSON.stringify(phase7),before);assert.equal(report.actual.anomalies,0);assert.equal(report.boundary.anomalies,0);assert.equal(report.randomValidation.anomalies,0);assert.equal(report.randomValidation.success,100000);assert.equal(report.summary.success,report.summary.executions);assert.equal(report.safety.passed,report.safety.cases);assert.equal(report.missingInputs.fallbackCount,report.missingInputs.cases);assert.ok(Object.values(report.mathematicalProperties).every(Boolean));assert.equal(report.assessment.robustness,'PASS');assert.equal(report.assessment.productionIntegration,'HOLD');assert.equal(report.assessment.accuracyEvaluated,false);
for(const file of['forecast-engine.js','forecast-features.js','forecast-ui.js','forecast-data.js'])assert.ok(!fs.readFileSync(`${root}/js/forecast/${file}`,'utf8').includes('MFLSpeciesPresenceCMRobustness'));
process.stdout.write(JSON.stringify({ok:true,version:MFLSpeciesPresenceCMRobustness.VERSION,summary:report.summary,assessment:report.assessment},null,2));
